# Homebrew tap for Marlin

Install [Marlin](https://github.com/jespern/marlin) on macOS or Linux:

```sh
brew install jespern/tap/marlin
```

Upgrade to the latest tagged release with:

```sh
brew upgrade marlin
```

Formula updates are published automatically by Marlin's release workflow.
